services:
  mysql:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: fraud_dashboard
      MYSQL_USER: fraud_user
      MYSQL_PASSWORD: fraud_pass
      MYSQL_ROOT_PASSWORD: local_root_pass
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 3s
      retries: 20
volumes:
  mysql_data:
